{
  "$schema": "../../../schemas/algorithm-spec.schema.json",
  "metadata": {
    "id": "hrv.tdmetrics",
    "module": "hrv",
    "algorithm_status": "stable",
    "specification_status": "draft"
  },
  "informative": {
    "title": "Time-domain beat or pulse variability metrics",
    "summary": "Computes standard time-domain HRV metrics from cleaned beat-to-beat or pulse-to-pulse intervals.",
    "description": "The input dtk is the interval series, in seconds, after beat or pulse detection, interval construction, and preprocessing for artifacts, missed beats, false detections, ectopic beats, outliers, and missing data. Invalid intervals may be removed before calling this algorithm, or retained as NaN markers that are omitted from metric calculations.",
    "notes": [
      "The canonical input is dtk, the cleaned beat-to-beat or pulse-to-pulse interval sequence.",
      "Intervals affected by artifacts, missed beats, false detections, ectopic beats, or outlier behavior should be detected and removed, corrected, or marked as NaN before calling the algorithm.",
      "NaN intervals are explicit missing or invalid interval markers and are ignored in all metric calculations, equivalent to omit-NaN behavior.",
      "The function must not silently treat invalid intervals as physiological variability.",
      "Removing invalid intervals or marking them as NaN can both be correct choices for time-domain statistics over valid intervals.",
      "tdmetrics does not interpolate NaN values, gap-fill intervals, or reconstruct event times internally.",
      "Canonical output IDs map directly in Python. In Biosigmat, pnn50 maps to pNN50 and the other output IDs map directly.",
      "Unresolved behavior: empty input.",
      "Unresolved behavior: one valid interval.",
      "Unresolved behavior: no valid successive intervals.",
      "Unresolved behavior: only one valid successive interval when sample standard deviation is requested."
    ],
    "keywords": [
      "dtk",
      "HRV",
      "time-domain",
      "missing data",
      "artifact handling",
      "wearable",
      "omitnan"
    ]
  },
  "provenance": {
    "references": [
      {
        "id": "task_force_hrv_1996",
        "relation": "metric_definition",
        "note": "Supports the conventional definitions and units of time-domain variability metrics; Biosiglib generalizes the timing input beyond ECG-only R-wave timing."
      },
      {
        "id": "cajal_missing_data_hrv_2022",
        "relation": "preprocessing_guidance",
        "note": "Supports robust handling of missing or invalid intervals when computing HRV metrics from wearable or artifact-affected interval series."
      }
    ]
  },
  "normative": {
    "inputs": [
      {
        "id": "dtk",
        "data_type": "real_vector",
        "shape": "vector",
        "unit": "s",
        "allow_nan": true,
        "allow_inf": false,
        "constraints": {
          "exclusive_minimum": 0
        }
      }
    ],
    "parameters": [],
    "outputs": [
      {
        "id": "mhr",
        "data_type": "real_scalar",
        "shape": "scalar",
        "unit": "beats/min"
      },
      {
        "id": "sdnn",
        "data_type": "real_scalar",
        "shape": "scalar",
        "unit": "ms"
      },
      {
        "id": "sdsd",
        "data_type": "real_scalar",
        "shape": "scalar",
        "unit": "ms"
      },
      {
        "id": "rmssd",
        "data_type": "real_scalar",
        "shape": "scalar",
        "unit": "ms"
      },
      {
        "id": "pnn50",
        "data_type": "real_scalar",
        "shape": "scalar",
        "unit": "%"
      }
    ],
    "definitions": [
      {
        "target": "dtk",
        "text": "dtk is the ordered vector of beat-to-beat or pulse-to-pulse intervals, expressed in seconds, after preprocessing for artifacts, missed beats, false detections, ectopic beats, outliers, and missing data."
      },
      {
        "target": "valid_interval",
        "text": "A valid interval is a finite, strictly positive, non-infinite, non-NaN element of dtk expressed in seconds."
      },
      {
        "target": "missing_interval_marker",
        "text": "NaN is an allowed missing or invalid interval marker in dtk and is omitted from all metric calculations."
      },
      {
        "target": "mhr",
        "text": "Mean heart or pulse rate is 60 / mean(valid dtk), where valid dtk is the sequence remaining after omitting NaN markers.",
        "latex": "\\mathrm{MHR} = \\frac{60}{\\operatorname{mean}(dTK)}"
      },
      {
        "target": "sdnn",
        "text": "SDNN is the sample standard deviation of valid dtk intervals after omitting NaN markers, using denominator N - 1, converted from seconds to milliseconds."
      },
      {
        "target": "successive_interval_differences",
        "text": "Successive interval differences are calculated between adjacent entries of the cleaned valid interval sequence after omitting NaN markers from dtk. NaN is not treated as zero and is not interpolated inside tdmetrics."
      },
      {
        "target": "rmssd",
        "text": "RMSSD is the square root of the mean squared successive interval differences from the cleaned valid interval sequence, converted from seconds to milliseconds."
      },
      {
        "target": "sdsd",
        "text": "SDSD is the sample standard deviation of successive interval differences from the cleaned valid interval sequence, using denominator N - 1, converted from seconds to milliseconds."
      },
      {
        "target": "pnn50",
        "text": "pNN50 is 100 * count(abs(successive interval differences) > 0.05 s) / number of successive interval differences in the cleaned valid interval sequence. The threshold is strictly greater than 50 ms."
      }
    ],
    "behavior": {
      "nan_handling": "NaN values in dtk are allowed and omitted from all metric calculations. Inf and -Inf intervals are invalid. Zero or negative finite intervals are invalid. tdmetrics must not silently interpolate, gap-fill, or treat invalid intervals as physiological variability.",
      "empty_input": "Empty dtk input is invalid; the exact failure mechanism is implementation-specific until an expected-error case is added.",
      "input_orientation": "Row and column vectors represent the same canonical dtk sequence and must produce scientifically equivalent outputs.",
      "insufficient_data": "Unspecified for too few valid intervals to calculate mean, sample standard deviations, or successive-difference metrics after omitting NaN markers."
    }
  }
}
