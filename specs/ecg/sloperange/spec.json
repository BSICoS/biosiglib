{
  "$schema": "../../../schemas/algorithm-spec.schema.json",
  "metadata": {
    "id": "ecg.sloperange",
    "module": "ecg",
    "algorithm_status": "stable",
    "specification_status": "draft"
  },
  "informative": {
    "title": "Slope-range ECG-derived respiration",
    "summary": "Estimates an ECG-derived respiration amplitude series from derivative ECG morphology around detected R waves.",
    "description": "The slope-range method summarizes beat-to-beat respiratory modulation by comparing the maximum upslope and minimum downslope of a derivative ECG signal in short windows around each R wave.",
    "notes": [
      "This first Biosiglib contract makes edr the only normative output.",
      "Diagnostic arrays exposed by Biosigmat, including upslopes, downslopes, upslope_max_position, and downslope_min_position, are informative implementation details at this stage.",
      "Biosigmat maps r_peak_times to its input currently named tk and sampling_frequency to fs.",
      "Implementation-specific array indexing conventions are not part of the Biosiglib contract."
    ],
    "keywords": [
      "ECG",
      "ECG-derived respiration",
      "EDR",
      "slope range",
      "respiratory modulation"
    ]
  },
  "provenance": {
    "references": [
      {
        "id": "kontaxis_edr_af_2020",
        "relation": "original_method",
        "note": "Primary method and provenance reference for slope-range ECG-derived respiration."
      },
      {
        "id": "varon_comparative_edr_2020",
        "relation": "validation",
        "note": "Comparative EDR context and validation evidence for single-lead ambulatory ECG."
      }
    ]
  },
  "normative": {
    "inputs": [
      {
        "id": "decg",
        "data_type": "real_vector",
        "shape": "vector",
        "unit": "a.u.",
        "allow_nan": false,
        "allow_inf": false,
        "constraints": {
          "minimum_length": 2
        }
      },
      {
        "id": "r_peak_times",
        "data_type": "real_vector",
        "shape": "vector",
        "unit": "s",
        "allow_nan": false,
        "allow_inf": false,
        "constraints": {
          "minimum_length": 1
        }
      },
      {
        "id": "sampling_frequency",
        "data_type": "real_scalar",
        "shape": "scalar",
        "unit": "Hz",
        "allow_nan": false,
        "allow_inf": false,
        "constraints": {
          "exclusive_minimum": 0
        }
      }
    ],
    "parameters": [],
    "outputs": [
      {
        "id": "edr",
        "data_type": "real_vector",
        "shape": "vector",
        "unit": "a.u."
      }
    ],
    "definitions": [
      {
        "target": "r_wave_sample_positions",
        "text": "Each R-wave time is mapped to the corresponding sample position on the derivative ECG sample grid using the sampling frequency. Implementations must evaluate the analysis windows around those sample positions while respecting their own array-indexing convention."
      },
      {
        "target": "analysis_windows",
        "text": "Set shortWindow = round(sampling_frequency * 0.015), longWindow = round(sampling_frequency * 0.05), upslopeWindow = -longWindow + 1 through shortWindow, and downslopeWindow = -shortWindow through longWindow - 1."
      },
      {
        "target": "edr",
        "text": "For each R wave with complete analysis windows, compute edr as max(decg over the upslope window) minus min(decg over the downslope window)."
      },
      {
        "target": "boundary_edr",
        "text": "For an R wave whose analysis windows extend outside the derivative ECG signal, preserve output alignment with r_peak_times and set the corresponding edr value to NaN."
      }
    ],
    "behavior": {
      "nan_handling": "NaN and infinite values in decg, r_peak_times, or sampling_frequency are invalid inputs. NaN values may appear in edr only to mark R waves whose analysis windows are incomplete at signal boundaries.",
      "empty_input": "Empty decg and empty r_peak_times inputs are invalid.",
      "input_orientation": "Treat decg and r_peak_times as one-dimensional vectors regardless of row or column orientation. The edr output is a one-dimensional ordered vector aligned with r_peak_times.",
      "insufficient_data": "If decg is too short to support complete windows around a beat, the affected boundary edr value is NaN when the corresponding R-wave sample position is inside the signal. R-wave times that map outside the derivative ECG sample grid are invalid."
    }
  }
}
